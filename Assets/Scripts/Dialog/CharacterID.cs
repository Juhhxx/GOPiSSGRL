using UnityEngine;

// characterID is not for each individuals characters lines, but for a stream of dialog
// that probably includes the player, and maybe more characters, but is only activated
// when interacting with the character or object that carries this ID...
public enum CharacterID
{
    Demon,
    Player,
    Clerk,
    Radio,
    Necro
}
